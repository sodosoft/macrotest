[system]
title = HD MACRO
version = 1.0.0
date = 2023-02-13

[position]
X = 916
Y = 362
box1_X = 0
box1_Y = 0
box2_X = 0
box2_Y = 0

; import configparser

; # 설정파일 만들기
; config = configparser.ConfigParser()

; # 설정파일 오브젝트 만들기
; config['system'] = {}
; config['system']['title'] = 'HD MACRO'
; config['system']['version'] = '1.0.0'
; config['system']['update'] = strftime('%Y-%m-%d %H:%M:%S')

; config['position'] = {}
; config['position']['X'] = '916'
; config['position']['Y'] = '362'
; config['position']['box1_X'] = ''
; config['position']['box1_Y'] = ''
; config['position']['box2_X'] = ''
; config['position']['box2_Y'] = ''

; # 설정파일 저장
; with open('config.ini', 'w', encoding='utf-8') as configfile:
;     config.write(configfile)
